# Flutter NHL 数据展示平台 - 实现计划

## 需求拆解

### 核心需求
1. **游戏列表屏幕** - 显示今天的所有游戏
   - 每行/卡片显示：主队名称和分数、客队名称和分数、游戏状态（scheduled/live/final）
   - 按开始时间排序

2. **游戏详情屏幕** - 点击游戏后打开
   - 显示该游戏文档的所有字段

3. **数据访问**
   - 所有数据来自 Firestore
   - 使用实时更新（streams/snapshots）

4. **必须处理**
   - 加载和错误状态
   - 缺失字段或 null 值不崩溃

### 可选功能
- 简单的离线行为
- 基本过滤器（如"只显示实时游戏"）

---

## 实现计划（按优先级和依赖关系）

### 阶段 1：项目基础设置 ⚙️
**目标**：配置项目依赖和基础架构

#### 1.1 添加依赖包
- [ ] `cloud_firestore` - Firestore 数据库连接
- [ ] `firebase_core` - Firebase 初始化
- [ ] `intl` - 日期时间格式化（可选，用于显示时间）

#### 1.2 Firebase 配置
- [ ] 创建 Firebase 项目（如果还没有）
- [ ] 下载并配置 `google-services.json` (Android)
- [ ] 下载并配置 `GoogleService-Info.plist` (iOS)
- [ ] 在 `main.dart` 中初始化 Firebase

**预计时间**：30-45 分钟

---

### 阶段 2：数据模型和类型定义 📦
**目标**：定义游戏数据结构和类型安全

#### 2.1 创建游戏数据模型
- [ ] 创建 `lib/models/game.dart`
- [ ] 定义 `Game` 类，包含：
  - 主队信息（名称、分数）
  - 客队信息（名称、分数）
  - 游戏状态（scheduled/live/final）
  - 开始时间
  - 其他可能字段（使用可选类型处理缺失字段）
- [ ] 实现 `fromFirestore` 和 `toFirestore` 方法
- [ ] 实现 `fromJson` 和 `toJson` 方法（用于调试）

#### 2.2 创建枚举类型
- [ ] 创建 `lib/models/game_status.dart`
- [ ] 定义 `GameStatus` 枚举（scheduled, live, final）

**预计时间**：45-60 分钟

---

### 阶段 3：Firestore 服务层 🔥
**目标**：封装 Firestore 数据访问逻辑

#### 3.1 创建 Firestore 服务
- [ ] 创建 `lib/services/firestore_service.dart`
- [ ] 实现获取今天游戏的方法：
  - 使用 `Stream<QuerySnapshot>` 实现实时监听
  - 查询条件：日期 = 今天
  - 按开始时间排序
- [ ] 实现获取单个游戏详情的方法
- [ ] 处理错误情况（网络错误、权限错误等）

#### 3.2 日期工具
- [ ] 创建 `lib/utils/date_utils.dart`
- [ ] 实现获取"今天"日期的工具函数（考虑时区）

**预计时间**：60-90 分钟

---

### 阶段 4：游戏列表屏幕 🎮
**目标**：实现主屏幕，显示今天的游戏列表

#### 4.1 创建列表屏幕
- [ ] 创建 `lib/screens/games_list_screen.dart`
- [ ] 使用 `StreamBuilder` 监听 Firestore 数据流
- [ ] 实现游戏卡片/列表项 UI：
  - 主队名称和分数
  - 客队名称和分数
  - 游戏状态显示（带颜色区分）
  - 开始时间显示
- [ ] 实现按开始时间排序
- [ ] 添加点击事件，导航到详情页

#### 4.2 状态处理
- [ ] 实现加载状态（显示加载指示器）
- [ ] 实现错误状态（显示错误消息和重试按钮）
- [ ] 实现空状态（没有游戏时显示提示）

#### 4.3 UI 组件
- [ ] 创建 `lib/widgets/game_card.dart` - 可复用的游戏卡片组件
- [ ] 创建 `lib/widgets/loading_indicator.dart` - 加载指示器
- [ ] 创建 `lib/widgets/error_widget.dart` - 错误显示组件

**预计时间**：2-3 小时

---

### 阶段 5：游戏详情屏幕 📋
**目标**：显示单个游戏的完整信息

#### 5.1 创建详情屏幕
- [ ] 创建 `lib/screens/game_detail_screen.dart`
- [ ] 接收游戏 ID 作为参数
- [ ] 使用 `StreamBuilder` 监听单个游戏文档
- [ ] 动态渲染所有字段：
  - 使用 `ListView` 或 `Column` 显示字段
  - 安全处理 null 值（使用 `??` 或条件渲染）
  - 格式化显示（日期、数字等）

#### 5.2 字段显示组件
- [ ] 创建 `lib/widgets/field_display.dart` - 通用字段显示组件
- [ ] 处理不同类型字段的显示（字符串、数字、日期、嵌套对象）

#### 5.3 导航设置
- [ ] 在 `main.dart` 中配置路由
- [ ] 实现从列表到详情的导航

**预计时间**：1.5-2 小时

---

### 阶段 6：错误处理和健壮性 🛡️
**目标**：确保应用不会因数据问题崩溃

#### 6.1 空值处理
- [ ] 在所有数据访问点添加 null 检查
- [ ] 使用安全导航操作符（`?.`）
- [ ] 为缺失字段提供默认值或占位符

#### 6.2 错误边界
- [ ] 在 StreamBuilder 中处理各种错误类型
- [ ] 实现用户友好的错误消息
- [ ] 添加重试机制

#### 6.3 数据验证
- [ ] 在模型类中添加数据验证
- [ ] 处理格式错误的数据

**预计时间**：1-1.5 小时

---

### 阶段 7：可选功能 - 过滤器 🔍
**目标**：添加基本过滤功能

#### 7.1 实现过滤器
- [ ] 在列表屏幕添加过滤选项（如：全部/仅实时游戏）
- [ ] 使用 `Chip` 或 `SegmentedButton` 作为过滤控件
- [ ] 在 Firestore 查询或客户端过滤数据
- [ ] 更新 UI 以反映过滤结果

**预计时间**：1 小时

---

### 阶段 8：可选功能 - 离线支持 📱
**目标**：实现基本的离线功能

#### 8.1 Firestore 离线配置
- [ ] 确保 Firestore 缓存已启用（默认启用）
- [ ] 测试离线场景下的数据访问
- [ ] 添加离线状态指示器（可选）

#### 8.2 离线体验优化
- [ ] 处理网络连接状态
- [ ] 显示离线提示（可选）

**预计时间**：30-45 分钟

---

### 阶段 9：UI/UX 优化 🎨
**目标**：提升用户体验

#### 9.1 视觉优化
- [ ] 添加合适的颜色主题
- [ ] 优化卡片布局和间距
- [ ] 添加状态图标（scheduled/live/final）
- [ ] 实现响应式设计

#### 9.2 交互优化
- [ ] 添加下拉刷新（可选）
- [ ] 优化加载动画
- [ ] 添加过渡动画

**预计时间**：1-2 小时

---

### 阶段 10：测试和调试 🧪
**目标**：确保应用稳定运行

#### 10.1 功能测试
- [ ] 测试正常数据流
- [ ] 测试缺失字段场景
- [ ] 测试错误状态
- [ ] 测试离线场景
- [ ] 测试过滤器功能

#### 10.2 边界情况测试
- [ ] 测试空列表
- [ ] 测试大量数据
- [ ] 测试网络中断
- [ ] 测试权限错误

**预计时间**：1-2 小时

---

## 项目结构建议

```
lib/
├── main.dart
├── models/
│   ├── game.dart
│   └── game_status.dart
├── screens/
│   ├── games_list_screen.dart
│   └── game_detail_screen.dart
├── services/
│   └── firestore_service.dart
├── widgets/
│   ├── game_card.dart
│   ├── loading_indicator.dart
│   ├── error_widget.dart
│   └── field_display.dart
└── utils/
    └── date_utils.dart
```

---

## 总预计时间

- **核心功能**（阶段 1-6）：6-8 小时
- **可选功能**（阶段 7-8）：1.5-2 小时
- **优化和测试**（阶段 9-10）：2-4 小时
- **总计**：9.5-14 小时

---

## 开发顺序建议

### 第一次迭代（MVP - 最小可行产品）
1. 阶段 1：项目基础设置
2. 阶段 2：数据模型
3. 阶段 3：Firestore 服务
4. 阶段 4：游戏列表屏幕（基础版本）
5. 阶段 5：游戏详情屏幕（基础版本）
6. 阶段 6：错误处理

### 第二次迭代（完善功能）
7. 阶段 7：过滤器
8. 阶段 8：离线支持
9. 阶段 9：UI/UX 优化
10. 阶段 10：测试和调试

---

## 关键注意事项

1. **数据安全**：确保所有字段访问都使用安全操作符
2. **性能**：使用 `StreamBuilder` 时注意重建优化
3. **用户体验**：始终显示加载和错误状态
4. **代码质量**：保持代码模块化和可维护性
5. **测试**：在不同设备和网络条件下测试

